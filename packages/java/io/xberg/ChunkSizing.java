// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:31f2498b6515e6b8fd195e375ca4fdd16109221b5bd8533f02d655f3d0bf9be4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import java.util.Optional;

/**
 * How chunk size is measured.
 *
 * Defaults to {@code Characters} (Unicode character count). When using token-based sizing,
 * chunks are sized by token count according to the specified tokenizer.
 *
 * Token-based sizing uses HuggingFace tokenizers loaded at runtime, or a tokenizer
 * backend you register yourself. Any tokenizer available on HuggingFace Hub can be
 * used, including OpenAI-compatible tokenizers (e.g., {@code Xenova/gpt-4o},
 * {@code Xenova/cl100k_base}). To size chunks with your own tokenizer instead (llama.cpp/GGUF
 * vocabularies, SentencePiece models, custom vocabs), register a {@code TokenizerBackend}
 * with {@code register_tokenizer_backend} and set {@code model} to the registered name.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = false)
@JsonSubTypes({
    @JsonSubTypes.Type(value = ChunkSizing.Characters.class, name = "characters"),
    @JsonSubTypes.Type(value = ChunkSizing.Tokenizer.class, name = "tokenizer")
})
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
public sealed interface ChunkSizing {

    /**
     * Size measured in Unicode characters (default).
     */
    record Characters() implements ChunkSizing {
    }

    /**
     * Size measured in tokens from a HuggingFace tokenizer or a registered
     * tokenizer backend.
     */
    record Tokenizer(
        @JsonProperty("model") String model,
        @JsonProperty("cache_dir") Optional<java.nio.file.Path> cacheDir
    ) implements ChunkSizing {
    }
}
