// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bfa8c57a0b7b5b5fcbf8b430f35ffc6666c57140b50e456be9fa217036d80967
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import Kreuzberg
import RustBridge

/// E2e tests for category: async.
final class AsyncTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _testDocs = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent() // <Module>Tests/
            .deletingLastPathComponent() // Tests/
            .deletingLastPathComponent() // swift/
            .deletingLastPathComponent() // packages/
            .deletingLastPathComponent() // <repo root>
            .appendingPathComponent("test_documents")
        if FileManager.default.fileExists(atPath: _testDocs.path) {
            FileManager.default.changeCurrentDirectoryPath(_testDocs.path)
        }
    }

    func testAsyncExtractBytes() async throws {
        // Async extract_bytes call on PDF document
        let contentData = try Data(contentsOf: URL(fileURLWithPath: "pdf/fake_memo.pdf"))
        let contentVec = RustVec<UInt8>()
        for _byte in contentData { contentVec.push(value: _byte) }
        let configObj = try extractionConfigFromJson("{}")
        let result = try await extractBytes(contentVec, "application/pdf", configObj)
        XCTAssertEqual(result.mimeType.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.toString().count, 50)
    }

    func testAsyncExtractBytesEmptyMime() async throws {
        // extract_bytes empty MIME async
        do {
            let contentData = try Data(contentsOf: URL(fileURLWithPath: "text/plain.txt"))
            let contentVec = RustVec<UInt8>()
            for _byte in contentData { contentVec.push(value: _byte) }
            let configObj = try extractionConfigFromJson("{}")
            _ = try await extractBytes(contentVec, "", configObj)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAsyncExtractBytesInvalidMime() async throws {
        // extract_bytes unsupported MIME async
        do {
            let contentData = try Data(contentsOf: URL(fileURLWithPath: "text/plain.txt"))
            let contentVec = RustVec<UInt8>()
            for _byte in contentData { contentVec.push(value: _byte) }
            let configObj = try extractionConfigFromJson("{}")
            _ = try await extractBytes(contentVec, "application/x-nonexistent", configObj)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
